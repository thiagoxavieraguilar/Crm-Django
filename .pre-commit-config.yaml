repos:
  - repo: local
    hooks:
      - id: black
        name: black
        entry: black
        language: system
        types: [python]
        additional_dependencies: [black]

      - id: isort
        name: isort
        entry: isort
        language: system
        types: [python]
        additional_dependencies: [isort]
